#!/usr/bin/env sh
# One command from a fresh clone: asks a few questions, writes .env, starts everything.
# Enter accepts every default; --yes skips the questions. Same file behind `npm start`.
set -eu
cd "$(dirname "$0")"
exec node scripts/start/main.ts "$@"
